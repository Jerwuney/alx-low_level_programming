#include "main.h"

/**
 * print_numbers - check for uppercase
 *
 * Return: Always 0 (executed)
 */

void print_numbers(void)
{

	int i;

	for (i = 0; i < 10; i++)
	{
		_putchar(i + '0');
	}
	_putchar('\n');

	return (0);
}
